
// Components
import HeaderNavbar from "./HeaderNavbar"

const Header = ( props ) => {
    return (
        <div className="header-container">
            <HeaderNavbar />
        </div>
    )
}

export default Header